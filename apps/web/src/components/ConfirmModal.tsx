/**
 * ConfirmModal — Glass-style confirmation dialog, replaces window.confirm()
 * Canonical implementation now consolidated and exported from @financeos/ui
 */
export {
  ConfirmModal,
  useConfirmModal,
} from '@financeos/ui';
export type {
  ConfirmModalProps,
  ConfirmModalState,
} from '@financeos/ui';
